# Simple Counter SPA (React + Vite)

A simple single-page React application built using Vite.  
It displays a counter with increment and decrement buttons, styled using basic CSS.

## 🚀 Features
- React functional component
- useState hook for state management
- Increment (+) and Decrement (−) counter
- Centered UI with nested boxes
- Clean and minimal design

## 🛠 Tech Stack
- React
- Vite
- JavaScript
- CSS

## 📸 UI Description

The app shows a counter value at the top, followed by plus and minus buttons, all enclosed within a white container and centered on a dark background.

## 👤 Author

Nikunj Jain
